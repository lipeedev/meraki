import "./all.mrk"

defn main() {
  val name = String.type
  IO.scan("What's your name? ", name)

  val length = String.length(name)
  val chars = String.chars(name)

  IO.printf("hi, my name is {}", name)
  IO.print("length: ", length)
  IO.print(chars)
}

main()
