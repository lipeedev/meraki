import "Array"
import "IO"
import "String"

task callback(String: element, Number: index) {
  def output = String.format("Element {} -> {}", index, element)
 
  return output
}

def array = Array.create("hello", "world")
def arrayMapped = Array.map(array, callback)

IO.print(arrayMapped)
